// Fixed for every test right now — not a configurable per-test field
// (we deliberately left test duration out of the test editor). If a
// future template needs a different pace, this becomes a per-test
// value instead of a constant.
export const QUESTION_TIME_SECONDS = 30;
